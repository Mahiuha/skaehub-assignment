#importing the modules required.
import unittest
from nose2.tools import params

class leap(unittest.TestCase):
    n = int(input("Enter Year: "))

    def is_year_leap(self):
        result = test.is_leap(year)

        